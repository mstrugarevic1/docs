# Lessons Learned

* This workspace root is not the docs Git repo; use `git -C docs ...` for repository status and diffs.
* `git -C docs push` can fail in the sandbox with GitHub DNS resolution; rerun with network escalation.
